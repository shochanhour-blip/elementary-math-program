# 小学校むけ さんすう教材

## プロジェクト概要
小学生（主に1〜3年生）向けの算数教材。ブラウザで動く単一HTMLファイル群。
先生や保護者が授業・家庭学習で使うことを想定。

## 技術方針
- ビルドツールなし、CDNなし、フレームワークなし
- 各教材は単一HTMLファイルで完結（HTML + CSS + JS をすべて1ファイルに）
- タッチ操作（iPad等）を前提としたUI設計
- `pointer events`（pointerdown / pointermove / pointerup）を使用。mouseイベントは使わない
- `setPointerCapture` でドラッグ中のポインタロストを防ぐ

## UI・デザイン方針
- テキストはひらがな優先（小学生が読めること）
- 派手すぎず、視認性を優先した配色
- アニメーションは控えめに（学習の妨げにならない程度）
- ボタンや操作要素は大きめ（タップしやすい）
- コメントは最小限。自明なことは書かない

## ファイル一覧
| ファイル | 内容 |
|---|---|
| index.html | トップページ（教材一覧） |
| kurainohyou.html | くらいのひょう（たしざん・ひきざん） |
| clock.html | とけいのがくしゅう |
| ikutsu.html | いくつといくつ（さくらんぼ計算） |
| allay.html | かけ算がくしゅう（アレイ図） |
| kuku.html | 九九のがくしゅう |
| division.html | わり算 |
| number_line.html | 数直線ドロー |
| number_tiles.html | かず タイル |
| polygon.html | 正多角形ドロー |

## kurainohyou.html の設計メモ
- たしざんモード：行Aと行Bに分割、タイルをドラッグで配置
- ひきざんモード：ONの間はタイルをタップすると斜線（removed状態）。専用の「ひく」ボタンはない
- タイルのplaceValue: 1000 / 100 / 10 / 1 / 0.1 / 0.01
- くりあがりゾーン：10枚そろったら上の位に繰り上げ確定
- state.tiles に全タイル管理、renderAll() で全体再描画

## clock.html の設計メモ
- モード1（とけいよみ）: 時計タップで数字表示を3段階サイクル（時のみ／分のみ／両方）
- モード2（かんさんARC）: 弧で分を視覚化、数直線パネル付き
- モード3（かんさんTILES）: タイルで時間・分を表現、ドラッグ編集可
- SVG座標系: 0°=12時方向、時計回り。clockPt(angleDeg, radius) で座標計算
- floatMins パターン: ドラッグ中は小数で蓄積、pointerup時に丸める
